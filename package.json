{
  "name": "porto-maull",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sass": "sass scss/style.scss style.css --style=compressed --source-map",
    "sass:watch": "sass scss/style.scss style.css --watch --style=compressed --source-map",
    "dev": "sass scss/style.scss style.css --watch --source-map",
    "build": "sass scss/style.scss style.css --style=compressed --no-source-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
